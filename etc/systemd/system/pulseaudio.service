[Unit]
Description=PulseAudio

[Service]
Type=forking
ExecStart=/usr/bin/pulseaudio --system --disallow-exit --disallow-module-loading=0 --daemonize --high-priority --resample-method=trivial
ExecStop=/usr/bin/pulseaudio --kill

[Install]
WantedBy=multi-user.target
